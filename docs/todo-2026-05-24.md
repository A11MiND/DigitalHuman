# TODO — 2026-05-24

## 角色系统架构

### 角色定义格式 `character.json`
```jsonc
{
  "id": "qin-shihuang",
  "name": "秦始皇嬴政",
  "icon": "portrait_thumb.png",        // 立绘小头像（80x80）
  "avatar_idle": "idle_5s.mp4",        // 5秒静态循环
  "avatar_talk": "talk_8s.mp4",        // 8秒说话动态
  "system_prompt": "你是秦始皇嬴政...",  // 系统提示词
  "tts_voice_id": "Cantonese_PlayfulMan",
  "tts_language": "Chinese,Yue",
  "theme_color": "#8A6D3B"             // 可选主题色
}
```

### Phase 1: 内置角色支持
- [ ] 重构 `server.py`: 从 `characters/` 目录加载角色
- [ ] 重构 `index.html`: 动态读取角色配置，替换硬编码
- [ ] 确保当前秦始皇功能不变

### Phase 2: 角色大厅页面
- [ ] 新建 `lobby.html` / `static/lobby.html`
- [ ] 展示所有可用角色卡片（立绘 + 名字）
- [ ] 点击角色 → 进入对话页面，携带角色 ID 参数
- [ ] 对话页面根据 `?char=qin-shihuang` 加载对应角色

### Phase 3: 导入/导出 .zip
- [ ] 导出：当前角色打包为 `.zip` (character.json + 资源文件)
- [ ] 导入：上传 `.zip` → 解压到 `characters/<id>/`
- [ ] 校验 `character.json` 必要字段
- [ ] 导入后刷新大厅

### 目录结构规划
```
characters/
  qin-shihuang/
    character.json
    portrait_thumb.png
    idle_5s.mp4
    talk_8s.mp4
```

### 技术注意
- video 资源通过 `/characters/<id>/idle_5s.mp4` 静态挂载
- TTS 音色列表需要对应 MiniMax 系统音色 ID
- 角色切换时需重置 conversation_history
