#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
  start|stop|restart)
    exec /usr/bin/systemctl "$1" digitalhuman.service
    ;;
  *)
    echo "Allowed actions: start, stop, restart" >&2
    exit 64
    ;;
esac

