# 数字人运维平台 · 规划清单

---

## ✅ 已经完成

| 项目 | 说明 | 地址 |
|------|------|------|
| 实时数据看板 | 对话量/角色热度/时段分布/延迟分析，OpenAI风格 | `/dashboard.html` |
| 日志查看器 | 实时滚动日志，颜色分级 | `/logs` |
| 对话数据库 | SQLite记录每次对话（时间/IP/内容/耗时） | API: `/api/conversations` |
| 服务健康检查 | 角色加载/API状态/TTS配置 | `/health` |
| 周报系统 | 每周一自动生成PDF+邮件 | `generate_report.py` |
| 小瑪老師优化 | 提示词修复/头像视频更新 | — |

---

## 📋 下一步规划

### P0 · 运维监控平台（合并为单一入口）
把 dashboard + logs + health 整合到 `https://digitalhumanpalace.duckdns.org:8443/ops` 一个页面

| 功能 | 状态 | 说明 |
|------|------|------|
| 中英双语切换 | 📝 待做 | 全局 `zh/en` 切换，所有文案翻译 |
| 服务状态总览 | 📝 待做 | 各服务运行状态/版本/运行时间 |
| 异常诊断面板 | 📝 待做 | 关联日志 → 分析原因 → 建议方案 |
| 日志集成 | 📝 待做 | dashboard 内嵌日志查看器+搜索过滤 |
| 系统资源监控 | 📝 待做 | CPU/内存/磁盘实时显示 |

### P0 · 异常诊断体系
每条异常显示：

| 字段 | 说明 |
|------|------|
| ⚠️ 状态 | 正常/警告/错误 |
| 📋 异常描述 | 具体什么错了 |
| 🔍 根因分析 | 可能的原因 |
| 💡 建议方案 | 怎么修 |
| 🔗 关联日志 | 直接跳转到相关日志行 |

### P1 · 运维 Chatbot
嵌入式对话助手，可以回答：
- "今天有多少对话？"
- "最近有没有异常？"
- "小瑪老師的角色配置在哪？"
- "帮我重启服务"（需确认）

### P1 · 快速操作面板
| 操作 | 说明 |
|------|------|
| 重启数字人服务 | `sudo systemctl restart digitalhuman.service` |
| 查看最近错误 | 最近20条ERROR日志 |
| 清理对话数据库 | 清空/归档旧记录 |
| 服务诊断 | 一键运行健康检查 |

### P2 · 高可用/负载均衡调研
由于当前单节点架构，可行的方案：

| 方案 | 复杂度 | 说明 |
|------|--------|------|
| **Nginx 反向代理** | ⭐⭐ | 前置nginx，可做缓存/限流/SSL终止 |
| **Docker 容器化** | ⭐⭐⭐ | 方便扩缩容，但当前单节点意义有限 |
| **多节点部署** | ⭐⭐⭐⭐ | 东京+新加坡双节点，前端nginx负载均衡 |
| **CloudFront CDN** | ⭐ | 静态资源加速，适合国内访问 |

推荐路径：Nginx 反代 → 容器化 → 多节点

---

## 🏗️ 架构建议

```
                             ┌─────────────────┐
                             │   Nginx (443)    │
                             │  SSL termination │
                             │   Rate limiting   │
                             └────────┬────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                  ▼
            ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
            │  FastAPI App  │  │   Static     │  │  Ops Monitor  │
            │  /ask /ws     │  │  /dashboard  │  │  /ops (合并)  │
            │  /api/*       │  │  /logs       │  │               │
            └──────────────┘  └──────────────┘  └──────────────┘
```

---

## 📝 讨论点

1. **Chatbot 直接操作服务器** → 需要确认安全边界：
   - 只读查询（状态/日志）✅ 可以
   - 写操作（重启/修改配置）→ 需要操作确认机制
   - 建议：先做只读问答，再加确认执行

2. **主站 `digitalhumanpalace.duckdns.org:8443`** → 这是已经配置了HTTPS的域名？需要我基于这个域名做新的运维入口？

3. **优先做哪个** → 我的建议顺序：
   - ① 双语运维平台（dashboard+logs合并+异常诊断）
   - ② 系统资源监控
   - ③ Chatbot（只读）
   - ④ 快速操作面板
   - ⑤ 高可用调研
